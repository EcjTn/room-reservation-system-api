spring:
  datasource:
    url: jdbc:postgresql://host:port/db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
recaptcha:
  secret: getOneFromGoogle